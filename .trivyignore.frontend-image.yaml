# Temporary, narrow vulnerability baseline (P5 follow-up).
#
# The frontend CONTAINER IMAGE has 9 findings beyond what
# .trivyignore.deps.yaml already covers for the npm dependency-scan (source
# tree fs scan vs. built node_modules resolve differently) -- kept in a
# SEPARATE file on purpose, since it scopes a different scan target. Includes
# CVE-2026-59873 (tar), CRITICAL severity -- flagged explicitly, not buried:
# same pre-existing/fixable/temporary/expiring treatment as every other entry
# here, not a lower bar for CRITICAL. Fixable (a fixed version exists per
# Trivy's own scan output), temporary: remediation is the same frontend
# dependency upgrade pass as .trivyignore.deps.yaml (P5 follow-up, item 1),
# tracked to land and this file deleted before 2026-09-02.
#
# Malformed YAML here must fail CI -- see the validate-ignore-files job.
vulnerabilities:
  - id: CVE-2026-13149
    purls: ["pkg:npm/brace-expansion"]
    statement: "pre-existing brace-expansion finding, container-image-only, fixable via the same frontend dependency upgrade pass as .trivyignore.deps.yaml; remediation required"
    expired_at: 2026-09-02
  - id: CVE-2026-14257
    purls: ["pkg:npm/brace-expansion"]
    statement: "pre-existing brace-expansion finding, container-image-only, fixable via the same frontend dependency upgrade pass as .trivyignore.deps.yaml; remediation required"
    expired_at: 2026-09-02
  - id: CVE-2026-69152
    purls: ["pkg:npm/brace-expansion"]
    statement: "pre-existing brace-expansion finding, container-image-only, fixable via the same frontend dependency upgrade pass as .trivyignore.deps.yaml; remediation required"
    expired_at: 2026-09-02
  - id: CVE-2026-69192
    purls: ["pkg:npm/ip-address"]
    statement: "pre-existing ip-address finding, container-image-only, fixable via the same frontend dependency upgrade pass as .trivyignore.deps.yaml; remediation required"
    expired_at: 2026-09-02
  - id: CVE-2026-33671
    purls: ["pkg:npm/picomatch"]
    statement: "pre-existing picomatch finding, container-image-only, fixable via the same frontend dependency upgrade pass as .trivyignore.deps.yaml; remediation required"
    expired_at: 2026-09-02
  - id: CVE-2026-48815
    purls: ["pkg:npm/sigstore"]
    statement: "pre-existing sigstore finding, container-image-only, fixable via the same frontend dependency upgrade pass as .trivyignore.deps.yaml; remediation required"
    expired_at: 2026-09-02
  - id: CVE-2026-59873
    purls: ["pkg:npm/tar"]
    statement: "pre-existing tar finding (CRITICAL), container-image-only, fixable via the same frontend dependency upgrade pass as .trivyignore.deps.yaml; remediation required"
    expired_at: 2026-09-02
  - id: CVE-2026-59874
    purls: ["pkg:npm/tar"]
    statement: "pre-existing tar finding, container-image-only, fixable via the same frontend dependency upgrade pass as .trivyignore.deps.yaml; remediation required"
    expired_at: 2026-09-02
  - id: CVE-2026-73566
    purls: ["pkg:npm/tar"]
    statement: "pre-existing tar finding, container-image-only, fixable via the same frontend dependency upgrade pass as .trivyignore.deps.yaml; remediation required"
    expired_at: 2026-09-02
