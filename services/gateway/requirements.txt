fastapi==0.141.1
uvicorn[standard]==0.34.0
httpx==0.28.1
redis==5.2.1
pydantic==2.10.4
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# config/roles.yaml loader (roles_config.py) — the RBAC permission source.
# Also used by mfa_config.py for config/mfa.yaml (the rollout mode/scope).
PyYAML==6.0.2

# MFA rollout (w8-planner-2). pyotp: RFC 6238 TOTP (mfa_totp.py). cryptography:
# AEAD envelope encrypt/decrypt for TOTP secrets (mfa_crypto.py), reusing
# libs/phi_crypto/envelope.py's encode/decode — matches
# libs/phi_crypto/requirements.txt's pin, same as records-service/
# intake-service already do for PHI field encryption (adr/0012).
pyotp==2.10.0
cryptography==50.0.1

# W10 Final Stage 6: real scrapeable Prometheus HTTP request metrics
# (libs/metrics/http.py::install_http_metrics).
prometheus-client==0.26.0
