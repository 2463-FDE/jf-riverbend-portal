fastapi==0.141.1
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
pydantic==2.10.4
httpx==0.28.1
# Stage 3: metadata-only tracing (libs/tracing) — real OTel SDK, only needed
# because this service now imports libs.tracing. See libs/tracing/requirements.txt.
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0

# W10 metrics Stage 3: the actual OTLP/HTTP exporter package libs/tracing/spans.py imports lazily when OTEL_EXPORTER_OTLP_ENDPOINT is set. Never installed anywhere before this stage — every prior OTEL_EXPORTER_OTLP_ENDPOINT reference in this repo silently fell back to the console exporter (the module's own documented degrade-on-ImportError path), so real export was never actually possible until now. Same version as opentelemetry-api/-sdk above; OTel's Python packages are versioned in lockstep.
opentelemetry-exporter-otlp-proto-http==1.29.0

# w8-planner-2 P2 (adr/0012): AEAD field encryption (libs/phi_crypto) for
# ssn/dob/notes at intake time. See libs/phi_crypto/requirements.txt.
cryptography==50.0.1


# for bedrock
# boto3==1.35.90