"""Shared, installable libraries used across services (opt-in via each
service's own requirements.txt) — distinct from the per-service application
code, which is deliberately not shared (see adr/0001).
"""
