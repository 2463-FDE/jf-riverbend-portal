# Real-provider SDKs — only needed by a service that actually selects
# LLM_PROVIDER=openai / anthropic / ollama / bedrock at runtime. Deliberately
# NOT referenced by the root requirements-dev.txt, so CI (which only
# exercises the fake provider) never installs or requires these.
openai==1.59.0
anthropic==0.42.0
httpx==0.28.1
boto3==1.35.90
