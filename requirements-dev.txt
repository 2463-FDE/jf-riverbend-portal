# Test/dev dependencies (superset of what the unit tests import).
pytest==8.3.4
redis==5.2.1
requests==2.32.3
httpx==0.28.1
pydantic==2.10.4
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
# db/policy_corpus_ingest.py imports this eagerly (it's a standalone script,
# not a lazy-import library module) — tests/test_policy_corpus_ingest_script.py
# needs it importable. Matches libs/rag_corpus/requirements.txt's pin.
pgvector==0.4.2
fastapi==0.115.6
PyYAML==6.0.2

# The Week-8 summary agent's loop, so its tests run the real create_agent graph.
# langchain-aws is NOT here: only the service itself builds ChatBedrockConverse.
langchain==1.3.16
langchain-core==1.6.0
